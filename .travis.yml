language: go

sudo: false
go:
  - 1.2

before_install:
  - go get
  - (cd interp && go get)
  - (cd gub && go get)
  - (cd cmd && go get)
  - (cd gub/cmd && go get)

script:
  - go test -v
  - (cd interp && go test -v)
  - (cd cmd && go build tortoise.go)
  # - go get "github.com/rocky/go-fish"
  - (cd trepan && go build)
  - cd gub
  - go build
  - go test -v
